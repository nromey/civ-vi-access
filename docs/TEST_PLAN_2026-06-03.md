Civ VI Access test results, 2026-06-03 build

How to use this file: each item below ends with a colon. Type your result on the line under it (what you heard, or "ok", or anything off). Save the file and tell me to pull it back, and I will read your notes and the matching Lua.log lines. No need to keep anything tidy.

Before testing: close game and launcher, relaunch the mod so these Lua/XML changes load. No launcher rebuild needed this time (only one C# change earlier, none tonight). If a feature does nothing, first confirm you are on the fresh build.

VERBOSITY AND TILE NAV (the big one)

1. Alt+V toggles verbosity in the world and says Verbose on / Verbose off (it is back on Alt+V, the Alt+B detour is gone):
says alalert still
2. In terse (default), move the cursor over tiles. You should hear a BRIEF line: terrain, coords, and any unit or city. No yields/appeal/continent/movement wall:
unable to switch (alt_++v) at all, I used to maybe be able to do it ... not now
3. Press Alt+V to go chatty, then move over tiles again. Now you should hear the FULL datasheet (yields, water, defense, appeal, movement, continent appended). Alt+V again returns to brief:

4. After a notification fires, does Alt+V still toggle, or does it say "Alert"? (Known issue. If it says Alert, note it. I added a log probe to find the right fix; just tell me it happened):
Still says alert
WHERE AM I AND COORDS

5. Press S on a tile. You should hear your bearing from capital AND the coordinates, ending in X then Y:
Yep, that works
6. Press Shift+S on a tile. You should hear the survey (bearing, terrain, nearest city) ending with the coordinates:
correct
7. Alt+S still speaks coordinates only (the quick one):
alt+s seems to speak what s speaks
SELECTED UNIT

8. Select your builder, then move the cursor onto the builder's own tile. You should hear the builder with "selected" after it, distinct from other units on the tile:
yes
9. With the builder selected, press S or Shift+S. You should hear "selected" plus the unit (e.g. selected French Builder), even if the cursor is on a different tile:
yep
BUILD PICKER v2 (Shift+B with a Builder selected)

10. Open it. The opening line should say how many are available and how many locked (e.g. "Build improvement. 2 available, 5 locked."):
yes
11. Arrow down past the available ones into the locked ones. Each locked entry should say "locked" and a reason (needs a tech, needs a civic, or requires a terrain like Hills):
I've heard tech and civic, some terrain speaks, but like for the offshore oil rig, you'd think you'd want "ocean" as well, sometimes it doesn't speak as much as it could
12. Press Enter on an AVAILABLE one. It should build and close:
yes
13. Press Enter on a LOCKED one. It should NOT build; it should explain what it needs and stay open:
yes
LOAD SCREEN WORDING

14. Start a brand new game. At the load screen you should still hear "Creating game":
yes
15. Load a save (or use Continue / resume). At the load screen you should now hear "Loading game" instead of "Creating game" (resume vs load both say Loading game for now):
I did a resume, I  still heard creating
FIRST CONTACT / DIPLOMACY (new, instrumented, first pass)

16. The fastest way to test without playing to a meet: press Alt+M (debug meet). It opens the vanilla diplo screen, which should trigger our options list:
pass, except for escape speaks and then you start hearing the beginnings of speech. Also, it speaks "tocangawa (spelling) of Japanese Empire, needs an article The
17. When a leader screen opens (real meet OR Alt+M), do you hear "Diplomacy with <leader> of <civ>. N options."?:
yes you hear the first option, unable to use arrows to move around.
18. Arrow Up/Down through the options. Each should read its name; unavailable ones should say "unavailable" and the reason:
unable toa rrow up/down
19. Press Ctrl+T while the options are up. Does it re-read the leader's greeting text?:
no, probably something to do with keystroke hangling (no arrows).
20. Press Enter on a safe option (e.g. a delegation or a friendly one, NOT war). Does it say "Selecting <name>" and do something, or "Couldn't complete that selection"? (Either way I read the log — this confirms the commit path):
na
21. Press Escape. Does it close the options list cleanly and leave you back at the game / vanilla screen?:
Escape does close options, it doesn't tell you it's closed but it does close.
22. If options never appear, just say so — the log will tell me whether extraction worked in that context (I included the support file directly, but this is the first live test of it):

GOODY HUT (only if you happen to enter a village)

23. Move a unit into a tribal village. What do you hear? (Even a generic line is fine; the handler logs the reward so I can lock the exact wording):

ANYTHING ELSE

24. Anything that sounded wrong, doubled, too chatty, too terse, or out of order:
I've noted that kinda stuff, did you end up working on civipedia? Guessing not.
